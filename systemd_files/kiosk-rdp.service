[Unit]
Description=RDP Session

[Service]
Type=oneshot
ExecStart=/usr/local/bin/kiosk-rdp
RemainAfterExit=yes   # ← Important
Restart=no            # ← Critical: let controller manage the loop
TimeoutStopSec=10

[Install]
WantedBy=default.target
